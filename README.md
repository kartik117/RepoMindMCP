# RepoMindMCP

Parses a GitHub repo into a Neo4j knowledge graph (functions, classes, files, calls, imports, inheritance) and exposes it as an MCP server, so Claude Desktop and Cursor can answer structural questions about a codebase in natural language.

## Status

Early build — in progress.

## License

MIT
